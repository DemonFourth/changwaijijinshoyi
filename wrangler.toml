name = "changwaijijinshoyi"
main = "workers/worker.js"
compatibility_date = "2024-01-01"

[env.production]
vars = { AUTH_ENABLED = "false", APP_PASSWORD = "", SESSION_SECRET = "" }

# D1 绑定在 Cloudflare Dashboard 中配置
# 创建 D1 后，在 Dashboard 中绑定为 DB